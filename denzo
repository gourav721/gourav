#include<stdio.h>
#include<stdlib.h>
int main()
{
   int x,p,amt=0;
   scanf("%d%d",&x,&p);
   p=100-p;
   while(x>0)
   {
       amt=amt+x;
       x=(p*x)/100;
   }
   printf("%d",amt);
   return 0;
}
